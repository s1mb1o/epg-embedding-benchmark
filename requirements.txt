numpy>=1.24

# Sentence Transformers backend (--api st)
sentence-transformers>=2.2.2

# FlagEmbedding backend (--api flag) — faster BAAI/bge-m3 inference
# 1.3.x breaks on transformers>=5 (missing is_torch_fx_available);
# 1.2.12+ breaks on Python 3.13 (missing Optional in trainer.py star-import);
# 1.2.11 works after patching trainer.py.
FlagEmbedding>=1.2.0,<1.3

# OpenAI backend (--api openai) — requires OPENAI_API_KEY env var
openai>=1.40.0

# Ollama backend (--api ollama) — requires local `ollama serve`
requests>=2.31.0
